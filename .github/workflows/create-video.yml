name: Test Video Workflow

on:
  issues:
    types: [opened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Test Step
      run: |
        echo "🎉 Workflow is working!"
        echo "This proves the workflow file is correct"
        date
        echo "If you see this, the YAML syntax is valid"
